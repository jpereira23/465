<%= form_for([@topic, @reference]) do |f| %>
  <% if @reference.errors.any? %> 
    <div id="error_explanation">
      <h2><%= pluralize(@rating.errors.count, "error") %> prohibited this reference from saving".</h2> 
      
      <ul> 
      <% @reference.errors.full_messages do |message| %> 
        <li><%= message %></li> 
      <% end %> 
      </ul> 
    </div> 
  <% end %> 
  
  <div class="field"> 
    <%= f.label :url %><br> 
    <%= f.text_field :url %><br>
       
  </div> 
  <div class="actions"> 
    <%= f.submit %> 
  </div> 
<% end %>
